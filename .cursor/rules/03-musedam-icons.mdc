---
description: MANDATORY for Icon handling. Enforces SVG-only maintenance and direct usage.
globs: **/*.{tsx,jsx,svg}
---

# MuseDAM Icon Standard (SVG-First)

## 1. 唯一存储规范

| 职责 | 路径 | 说明 |
|------|------|------|
| SVG 文件 | `public/icons/[Name].svg` | 通过 HTTP `/icons/[Name].svg` 访问 |
| Icon 组件 | `src/components/icons/index.tsx` | 导出 `<Icon>` 和 `IconProps` |
| 导航图标映射 | `src/components/icons/NavIcons.tsx` | 导出 `<NavIcon>` 和 `NavIconName` |

**NEVER:**
- NEVER store SVG files under `src/` (deprecated: `src/components/icons/*.svg`)
- NEVER create individual `XxxIcon.tsx` wrapper files
- NEVER use `SvgIcon`, `UnifiedIcon` (deleted)
- NEVER import from `src/components/ui/icon.tsx` or `src/components/ui/icons.tsx` directly

## 2. 标准使用方式

```tsx
// ✅ 正确 — 所有业务组件统一使用这个
import { Icon } from '@/components/icons';
<Icon name="CaretDown" size={16} className="text-foreground" />

// ✅ 导航图标（仅 IconRail 使用）
import { NavIcon } from '@/components/icons/NavIcons';
<NavIcon name="approvalCenter" size={16} />

// ❌ 错误 — 不要使用旧路径
import { SvgIcon } from '@/components/ui/icon';
import { DashboardIcon } from '@/components/ui/icons';
```
## 3. Figma → SVG 工作流

### 3.1 【强制】图标名称锚定规则
- Figma MCP 返回的代码中，图标节点会带有 `data-name="XxxName"` 属性
- **MUST use the exact `data-name` value as filename and `<Icon name>`. NEVER infer, rename, or substitute.**
- ✅ `data-name="SquarePen"` → `SquarePen.svg` → `<Icon name="SquarePen" />`
- ❌ `data-name="User2"` → MUST NOT be written as `UserOutlined` / `User` / `UserIcon`

### 3.2 提取步骤
1. **Extract**: 使用官方 `figma` MCP 的 `get_design_context` 工具获取原始 SVG 内容
2. **Cleanup**: 必须处理 SVG：
   - 移除硬编码颜色 → 统一替换为 `fill="currentColor"` 或 `stroke="currentColor"`
   - 移除固定 `width`/`height` 属性（由 `<Icon size={n}>` 控制）
   - 保留 `viewBox`
3. **Save**: 直接保存到 `public/icons/[Name].svg`
4. **Use**: `<Icon name="[Name]" size={16} className="text-foreground" />`

## 4. 图标查找优先级

1. **检查 `public/icons/`** — 确认同名 SVG 是否已存在
2. **从 Figma 提取** — 按上述工作流操作
3. **lucide-react fallback** — 仅当 `public/icons/` 中确实不存在时才使用

## 5. SVG 文件规范

```svg
<!-- ✅ 正确示例 -->
<svg viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg">
  <path fill="currentColor" d="..."/>
</svg>

<!-- ❌ 错误：硬编码颜色和尺寸 -->
<svg width="16" height="16" fill="#1A1A1A" ...>
```
