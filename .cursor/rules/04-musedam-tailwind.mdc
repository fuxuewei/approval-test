---
description: MuseDAM Tailwind v4 best practices. CSS-first configuration with @theme directive.
globs: **/*.{tsx,jsx,css}
---

# MuseDAM Tailwind CSS v4 Production Rules

> **Version**: Tailwind CSS v4.x (CSS-first configuration)
> **Config Location**: `src/app/globals.css` via `@theme` directive

## 0. Tailwind v4 Configuration Model

Tailwind CSS v4 采用 **CSS-first 配置**，所有主题配置通过 `@theme` 指令在 `globals.css` 中定义：

```css
@import "tailwindcss";

@theme {
  /* 颜色通过 CSS 变量映射 */
  --color-background: var(--background);
  --color-primary-5: var(--color-primary-5);
  --color-primary-6: var(--color-primary-6);
  /* ... */
}
```

**注意**: `tailwind.config.ts` 仅保留 `content` 配置用于扫描路径，不再使用 `theme.extend`。

---

## 1. Color Rules (MANDATORY - Simplified Syntax)

所有颜色必须使用简化的 Tailwind 类。禁止任意值语法如 `text-[var(--xxx)]` 或 `text-[#3366FF]`。

### Primary Scale (1,2,5,6,7)

| Class | 色值 | 用途 |
| --- | --- | --- |
| `text-primary-6` | #3366FF | 主品牌色 |
| `text-primary-5` | #598BFF | hover 状态 |
| `text-primary-7` | #274BDB | active 状态 |
| `bg-primary-1` | #F2F6FF | 浅色背景 |
| `bg-primary-2` | #D9E4FF | 次级浅色背景 |
| `bg-primary-6` | #3366FF | 主色填充 |

### Semantic Colors

| 语义 | 文字 | 背景 | Hover/Active |
| --- | --- | --- | --- |
| Success | `text-success-6` | `bg-success-1` | `text-success-7` |
| Warning | `text-warning-6` | `bg-warning-1` | `text-warning-7` |
| Danger | `text-danger-6` | `bg-danger-1` | `text-danger-7` |
| Info | `text-info-6` | `bg-info-1` | `text-info-7` |

### Core Colors

```text
bg-background     → 页面背景 (var(--background))
bg-card            → 卡片背景 (var(--card))
bg-popover         → 浮层背景 (var(--popover))
text-foreground    → 主文字 (var(--foreground))
text-basic-5 → 次级文字 (var(--muted-foreground))
border-border      → 边框颜色 (var(--border))
```

### Legacy Aliases (向后兼容)

- `text-primary-600` = `text-primary-6`
- `bg-primary-100` = `bg-primary-1`
- `text-success-600` = `text-success-6`
- `bg-success-100` = `bg-success-1`
- 其他语义色同理

### Strictly Forbidden

```text
❌ text-[#192038]          → 使用 text-foreground 或 text-text-primary
❌ bg-[#3366FF]            → 使用 bg-primary-6
❌ text-[var(--ant-primary-6)] → 使用 text-primary-6
❌ text-[var(--color-primary-5)] → 使用 text-primary-5
❌ muse-* 旧颜色前缀
❌ Tailwind 默认灰色 (gray-500 等)
```

---

## 2. Spacing & Layout

### Gap-based Layout (Mandatory)

- 仅使用 `gap-1` (4px) 到 `gap-12` (48px)
- 容器布局：`flex` + `gap-*` + `p-4/p-6`
- NEVER use `m-*` margin between sibling elements
- 内容区：`max-w-7xl mx-auto` 或 `w-full`
- 如果 Figma 间距不是 4 的倍数，映射到最近的合法 token；若会造成明显视觉偏差，必须在 `Unmapped tokens` 中记录原值和使用位置

### Example

```tsx
// ✅ 正确 - 使用 gap
<div className="flex gap-4 p-4">
  <div>...</div>
  <div>...</div>
</div>

// ❌ 错误 - 使用 margin
<div className="flex p-4">
  <div className="mr-4">...</div>
  <div>...</div>
</div>
```

---

## 3. Border Radius

| 组件 | 类名 |
| --- | --- |
| Button/Input | `rounded-button` (6px) |
| Card | `rounded-card` (8px) |
| Modal/Dialog | `rounded-modal` (12px) |
| Tag | `rounded-tag` (4px) |
| Avatar | `rounded-avatar` (50%) |
| 默认 | `rounded-md` |

---

## 4. Typography

| 层级 | 尺寸 | 行高 | 字重 |
| --- | --- | --- | --- |
| Page title | `text-22` | `leading-heading` | `font-semibold` |
| Card title | `text-20` | `leading-heading` | `font-semibold` |
| Section title | `text-18` | `leading-heading` | `font-semibold` |
| Button/Label/Table header | `text-14` | `leading-body` | `font-medium` |
| Body | `text-14` | `leading-body` | normal |
| Helper/Caption | `text-13` | `leading-body` | `text-basic-5` |

### Font Size Classes

```text
text-12, text-13, text-14, text-16, text-18, text-20, text-22,
text-26, text-30, text-32, text-36
```

### Line Height Classes

```text
leading-heading-tight (1.2)
leading-heading (1.3)
leading-body (1.5)
```

### Typography Mapping (Mandatory)

- 对每个主要文本块，必须逐项映射 `font-size`、`line-height`、`font-weight`、`letter-spacing`
- 不允许仅凭视觉估计从 `text-14` / `text-16` 中二选一
- 如果现有类名无法表达 Figma 排版值，先输出 `Unmapped tokens`，不要私自创造近似类

---

## 5. Dark Mode Support

ALL colors MUST use simplified Tailwind classes. NEVER use arbitrary-value syntax like `text-[var(--xxx)]` or `text-[#3366FF]`.

```tsx
<div className="bg-background text-foreground dark:bg-background dark:text-foreground">
  {/* 自动适应主题 */}
</div>
```

CSS 变量在 `:root` 和 `.dark` 中分别定义了 Light/Dark 值。

---

## 6. Effects & Surfaces

- 卡片默认仅边框，不加阴影
- Dropdown / Popover / Modal 可使用 `shadow-[var(--shadow-dropdown)]` 或等价封装
- Border、radius、opacity 必须优先使用项目 token；无法映射时先记录到 `Unmapped tokens`

---

## 7. Reference Files

- **Theme Config**: `src/app/globals.css` (v4 `@theme` 指令)
- **Legacy Config**: `tailwind.config.ts` (仅保留 content 扫描路径)
- **Variables**: `:root` 和 `.dark` 中的 CSS 自定义属性
