---
description: Auto-detect and install missing shadcn/ui components via CLI during reskin.
globs: **/*.{tsx,jsx}
---

# MuseDAM shadcn/ui Auto-Installer Skill

## Trigger Conditions
- During reskin (02) or new component generation
- Figma mentions a standard component (Button, Input, Dialog, Table, Select, Tabs, Card, Badge, Alert, etc.)

## Mandatory Flow
1. Scan Figma description / layer names for shadcn primitives.
2. Check if file exists in src/components/ui/[name].tsx
3. If missing:
   - First output exact CLI command the user should run: