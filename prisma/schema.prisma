generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  // 相对 prisma/ 目录；生产可改为 env("DATABASE_URL") + PostgreSQL 等
  url      = "file:./dev.db"
}

// 占位：接入真实业务模型前请替换或删除
model SchemaPlaceholder {
  id String @id @default(cuid())
}
